import 'package:flutter/material.dart';

class CourseOverviewCharts extends StatelessWidget {
  const CourseOverviewCharts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
