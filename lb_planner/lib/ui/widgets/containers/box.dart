import 'package:flutter/material.dart';
import 'package:lb_planner/ui.dart';

class NcBox extends StatelessWidget {
  const NcBox({Key? key, required this.text}) : super(key: key);

  final String text;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
