import 'package:flutter/material.dart';
import 'package:lb_planner/ui.dart';

class Nc2GridView extends StatelessWidget {
  const Nc2GridView({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
      child: Wrap(),
    );
  }
}
