part of lbplanner_routes;

/// Dropdown for [CalendarPlanRoute].
class CalendarPlanDropdown extends StatelessWidget {
  /// Dropdown for [CalendarPlanRoute].
  const CalendarPlanDropdown({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return LpContainer(
      width: 500,
      height: 500,
      child: NcTitleText("test"),
    );
  }
}
