part of lbplanner_routes;

/// User notifications item.
class UserNotificationsItem extends StatelessWidget {
  /// User notifications item.
  const UserNotificationsItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
